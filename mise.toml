[tools]
node = "24.15.0"
pnpm = "11"

[tasks.setup]
run = [
  "pnpm install",
  "git submodule update --init",
  "for dir in corpus/fixtures/*/; do if [ -f \"$dir/pnpm-lock.yaml\" ]; then pnpm --dir \"$dir\" install --frozen-lockfile; fi; done",
]
