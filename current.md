---
date: 2026-07-18
status: 第 13 版 (評価データの二本柱: 10 パターン紐づき 16 issue = 生成手法の正確性 / 残り 81 issue = 拡張性)
goal: 投稿先・時期は質を優先して再検討中 (締切は置かない)
supersedes: versions/v12_2026-06-23.md
---

# 研究計画

> **本論文の核は「条件 (適用条件) の自動抽出」である．** 構造パターンの導出は前提として置き，研究の中心は「(before, after) ペアからどんな条件で安全に適用できるかを自動で取り出すこと」に統一する．構造パターンの精密化は将来課題に降ろす．

---

## 背景

### 未解決問題: (before, after) ペアから「条件付き検出パターン」を自動導出する

「遅い書き方と速い書き方のペアから，**どこでその改善が使えるか**を自動検出する」ためには，**構造と条件の両方**が必要:

| 側面 | 問い | 例 (Pattern #1: for..in → Object.keys) |
|---|---|---|
| **構造** | どの AST ノードがパターンに必須か | `for..in` + `hasOwnProperty` ガードが必須．変数名や本体は自由 |
| **条件** | どんな条件で安全に適用できるか | 対象が Object 型 + prototype が汚染されていない |

**構造だけでは FP だらけ．条件だけでは検出対象がない．両方揃って初めて使える検出器になる．** 本論文は**②条件の自動抽出を核**に据え，①構造は導出済みの前提として置く (精密化は future work)．

### Hydra の解法: SMT による記号実行

**Hydra (OOPSLA 2024)** は LLVM IR で**構造と条件の両方を N=1 で自動導出**した．SMT ソルバ (論理式の成立を全入力について自動判定するツール) を使い，各 AST ノードを抽象化しても等価性が保たれるかを**全入力に対して**検証する (pruning)．

Hydra が可能だった理由: **LLVM IR の整数演算は SMT の決定可能な断片に収まる**ため，全入力を記号的に網羅できる．

### JavaScript で同じことができない理由

JS の動的意味論 (動的型付け，prototype chain，closure，`eval` 等) は SMT で完全にモデル化できず，**記号実行のパスが無限に爆発する**．

ただし JS 向けの記号実行ツール (ExpoSE 等) は存在し，JS の**部分的な断片** (単純な算術，文字列操作等) は記号的に扱える．問題はパスが有限に収まらないこと．

### SYDIT の教訓: 構造だけでは不十分

**SYDIT (FSE 2011)** は N=1 で構文パターンの導出を試みたが，**条件を付けなかった**ため FP が多すぎた．**LASE (ICSE 2013)** で同著者が「N=1 は精度不足」と批判し，以降は複数例前提の研究 (Getafix OOPSLA 2019, REVISAR ICSE 2018, PyCraft FSE 2024) に流れた．条件付けこそがボトルネックだったことを示す系譜であり，本論文が条件の自動抽出を核に据える根拠でもある．

### Selakovic の手動前提条件 (= 本研究の背骨)

**Selakovic & Pradel (ICSE 2016)** は 16 の JS プロジェクトから 98 件の修正済みパフォーマンス issue を収集し，10 の再帰パターンに **5 種類の前提条件** (T/NF/P/TF/V) を手動で付与した．§7.2 で「**自動チェックは困難であり今後の課題**」と結論．**この「手動前提条件の自動化」が本論文の中心的ギャップ (背骨) である．**

データセット: https://github.com/marijaselakovic/JavaScriptIssuesStudy

### 10 年間誰も取り組んでいない (調査済み)

被引用 114 件の全件確認 + 広域検索の結果，JS で構造の自動導出も条件の自動チェックも行った後続研究は **0 件**．

---

## 課題

JS で「条件付き検出パターンの自動導出」を実現するには:

1. **構造の導出**: どの AST ノードがパターンに必須かを判定 (本論文では導出済みの前提．精密化は future work)
2. **条件の特定**: どんな条件で安全に適用できるかを判定 (**本論文の核**)
3. **条件による記号実行パスの有限化**: JS 全体の記号実行は無理だが，条件で入力空間を分割すればパスを有限にできる可能性がある

Hydra は 1〜3 を SMT で一括解決した (LLVM IR だから可能)．JS ではこれを**構造 (課題 1・前提) と条件抽出 (課題 2〜3 を一体で解く・本論文の核) の 2 段階**に分けて解く．条件の特定 (課題 2) と記号実行パスの有限化 (課題 3) は「条件軸で入力空間を有限分割する」同一の営みであり，条件抽出フェーズの中で一体に扱う．

---

## 目的

> **(before, after) ペアから条件付き検出パターンを自動導出する手法を段階的に確立する．本論文ではとくに「適用条件の自動抽出」を中心に据える．**

**抽出対象の射程 (確定・射程 B)**: 抽出する「適用条件」は，この特定インスタンスが等価な条件 (候補 A) ではなく，**そのパターン (防御的ガードを剥いだ核変換) の前提条件** (候補 B) である．理由は 3 点: (1) Selakovic Table 4 の条件がパターン単位なので，正解合わせには核変換の条件が要る，(2) 検出ルール (eslint) の適用先はガードの無い候補箇所なので，そこで必要なのはパターン条件，(3) 論文の核「Selakovic の手動**前提条件**の自動化」と一致する (Selakovic が手付けしたのはパターンの前提条件)．**成果物は「ガードを除いた核変換の構造 + その核条件」の組**とし，構造と条件が同じ核変換を指すので整合する (§step B)．ガードは「その条件を discharge していたもの」として結果に報告する．インスタンス自体が (ガードのおかげで) 安全でも核変換条件を報告できるのはこのため．

---

## アプローチの 2 段階構成

本研究は問題を 2 段階で解く．**第 1 段階 (構造) は導出済みの前提として置き，第 2 段階 (条件抽出) を本論文の核として実装・評価する．構造パターンの精密化は future work とする．**

```
第 1 段階: 構造パターンの自動導出 (導出済み・前提)
  (before, after) ペアから実行ベースの pruning で構文パターンを導出
  → 出力は over 方向を意図 (余分を含む・取りこぼしゼロは保証しない)

第 2 段階: 条件抽出 — 同値分割テストによる適用条件の自動抽出 (本論文の核)
  Selakovic の T/NF/P/TF/V を目標語彙に，構造パターンへ適用条件を付与する．
  内部は 2 step:
    step A 分割設計: パターンのスロットに効きそうな条件軸を当て，各軸を
                     有限クラスに割り，各クラスの代表値を合成する (候補・仮説)
    step B 検証実行: 代表値を EMIC で走らせ，等価性が壊れた軸を条件として確定する
```

### 段階と step の関係

| 段階 / step | 前提 | 実現すること | 出力 |
|---|---|---|---|
| **第 1 段階** 構造導出 | なし | 構造パターン導出 (over 方向を意図・保証せず) | ワイルドカード付き AST パターン (余分な枝を含む) |
| **第 2 段階 / step A** 分割設計 | 第 1 段階のパターン | 効く条件軸の候補出し + 有限クラス分割 + 代表値合成 | パターン + 各軸の分割表 (結果は空欄・仮説) |
| **第 2 段階 / step B** 検証実行 | step A の分割表 | 代表値で EMIC を走らせ等価性破壊を観測 | パターン + 確定した T/NF/P/TF/V 条件 + 各クラスの検証結果 |

依存関係:
- 第 2 段階はパターンがないと条件を付ける対象がない (第 1 段階が前提)
- step B は step A の分割表がないと走らせる代表値がない
- **条件の確定は step B で起きる**: step A は「何を疑うか」の候補にすぎず，「実際に効く条件」は走らせて初めて分かる

> **条件抽出を 1 フェーズ 2 step で表現する理由**: 分割設計と検証実行は実装上ひとつのモジュール (パイプライン [2]) であり不可分 (走らせないと条件は確定せず・分割しないと走らせられない)．対等な別段階に並べると境目が曖昧になるため，**条件抽出フェーズの中の設計→検証の 2 step** として表現する．

---

## アプローチ

### パイプライン

```
入力: (before, after) ペア + セットアップコード

  [1] Pruning → 構造パターン導出 (第 1 段階)
       ※ 出力は over 方向を意図: 余分な枝を含む (取りこぼしゼロは保証しない)
  [2] T/NF/P/TF/V 条件抽出 (第 2 段階: step A 分割設計 → step B 検証実行) ← 本論文の核

出力 (成果物): core 構造 (ガード除去) + 核条件 (Level 別の確信度付き)

※ EMIC (入力限定等価確認器・4 オラクル) は [1] と [2] の内部で使う共通基盤
※ 検出ルール (ts-eslint) の自動生成は本論文スコープ外 → §Future Work
```

### EMIC (入力限定等価確認器): 4 オラクル (O1〜O4)

`before ≡ after` を有限の代表入力に対して判定する基盤ツール．名称・改名根拠は §用語の再検討を参照．Node.js の `vm` で隔離実行し，以下 4 軸で出力を観測する．

| # | オラクル | 観測内容 | 必要な理由 (Selakovic パターン) |
|---|---|---|---|
| **O1** | 戻り値 | 式の評価結果 (deep equal) | for..in の列挙結果，`String()` の文字列化結果 |
| **O2** | 引数変異 (副作用の観測) | 入力オブジェクトの実行前後の構造差分 | Array 操作 (push/splice) 等の破壊的メソッドの影響 |
| **O3** | 例外 | throw の有無・型・メッセージ | prototype 汚染で `hasOwnProperty` が壊れた時の TypeError |
| **O4** | 外部観測 (副作用の観測) | console / グローバル変数 / I/O の差分 | DOM/I/O 系パターン (Selakovic では少数) |

イテレーション順序・Promise 解決値は O1 に集約 (配列に集める / `await` 後の値を比較)．時間計算量は等価性の対象外．

**副作用の観測 (O2/O4) の実現方式は spike で決定する (実装方式未定)．** 現時点では「実行前後の状態差分を取る」方式が有力 (`open-questions.md` に登録済み)．

#### 条件 (T/NF/P/TF/V) と O1〜O4 (オラクル) の関係

条件軸と O は直交する別概念．**条件は入力空間を分割し，O は出力を観測する．**

| | 条件 (T/NF/P/TF/V) | O1〜O4 |
|---|---|---|
| 役割 | 入力空間の分割 | 出力の観測 |
| 問い | どんな入力で検証するか | 何を見て等価と判定するか |
| 出現段階 | 第 2 段階 (条件抽出) | 全段階の EMIC 内部 |

EMIC の内部で両者は掛け合わさる:

```
for 各 条件軸のクラス:
  代表値 v を選ぶ
  result_before = run(before, v)   ← O1〜O4 を観測
  result_after = run(after, v)   ← O1〜O4 を観測
  if any O_i で result_before ≠ result_after → そのクラスで非等価
```

<details>
<summary><strong>具体例: Pattern #1 の P (prototype 上書き) 軸 × オラクル</strong></summary>

| P/NF クラス | 代表値 | O1 戻り値 | O3 例外 |
|---|---|---|---|
| clean proto | `{a:1}` | 等価 | 等価 |
| 汚染 proto (P 違反) | `Object.create({x:1})` | **非等価** (before が `x` も列挙) | 等価 |
| `hasOwnProperty` 上書き (NF 違反) | `{hasOwnProperty:()=>true}` | 等価 | **非等価** (before が throw) |

→ 上書き検査軸 (NF/P) を有限クラスに切り (条件)，各クラスで何が壊れるかを 4 オラクルで観測 (O) する．

</details>

### 第 1 段階: 実行ベース Hydra 式 pruning (over 方向を意図・保証せず)

Hydra の SMT ベース pruning を，JS では**実行ベースの EMIC** で代替する．さらに **AST 差分フィルタ**を前段に置き，単一セットアップでの誤 prune を構造的に防ぐ．

**pruning の出力は over 方向への近似を意図する (設計意図であって保証ではない)**: 「消しても等価性が保たれるか」でしか削れないため，本来は取りこぼさず**改善の核以外の文脈 (ループの殻等) が余分に残る**側に倒れる想定である．ただし判定は有限の代表セットアップ上の EMIC なので，**セットアップが必要性を発揮しない入力しか含まないと，必要ノードを落とす (under-approximation) 誤りもありうる**．差分フィルタは差分ノードを必須保護するが，共通ノードは pruning 試行対象なので落としすぎのリスクが残る．構造は本論文の**前提**であり，**構造段階の健全性 (取りこぼしゼロ) は保証しない**: over 側に倒れているかは §事前分析の**内包率で経験的に測る**．過剰量も定量報告する．核を絞る精密化は future work とする．

**AST 差分フィルタ** (Babel AST 上で GumTree top-down phase [Falleri+ 2014] を実装):
- before のノードのうち、after に同型サブツリーが存在しないもの = **差分ノード**
- 差分ノードは「after に対応物がない = パターンの本質」として pruning 候補から除外 (試行せず必須扱い)
- 共通ノード (after にも同型存在) のみを pruning 候補にする

**pruning 本体**:

```
差分フィルタ通過後の候補ノード N について:
  - N をリネーム ($VAR) または削除して before' を作る
  - EMIC で before' ≡ after を setup 上で確認
  - 等価 → N は不要 → ワイルドカード化
  - 非等価 → N は必須 → 残す
→ 必須ノードだけ残った最小構文パターンが出力 (ただし over 方向を意図・保証ではない)
```

初回の検証 (pruning 前の before ≡ after) で等価性が確認できなければ，そのペアは不正として除外される．**事前の等価性検証ステップは pruning に吸収される**．

<details>
<summary><strong>具体例: Pattern #1 (for..in → Object.keys)</strong></summary>

```js
// before (元)
for (var key in obj) {
  if (obj.hasOwnProperty(key)) { use(key); }
}
```

| ノード | 差分フィルタ判定 | pruning 試行 | 判定 |
|---|---|---|---|
| `var` | 共通 (候補入り) | ワイルドカード化可 | **不要** → `$DECL` |
| `key` | 共通 (候補入り) | リネーム可 | **不要** → `$ITER` |
| `obj` | 共通 (候補入り) | リネーム可 | **不要** → `$TARGET` |
| `use(key)` | 共通 (候補入り) | 削除/置換可 | **不要** → `$BODY` |
| `hasOwnProperty` チェック | **差分 (after に非存在)** | 試行せず | **必須** |

自動導出結果:
```
for ($DECL $ITER in $TARGET) {
  if ($TARGET.hasOwnProperty($ITER)) { $BODY }
}
```

</details>

### Hydra (SMT) との違い

| | Hydra | 本研究 |
|---|---|---|
| 対象言語 | LLVM IR | JavaScript |
| 検証手段 | SMT (**全入力を保証**) | 実行ベース (**有限クラスの代表値で検証**) |
| AST 差分解析 | なし (SMT が一括解決) | Babel AST + GumTree top-down phase [Falleri+ 2014] |
| 構造出力 | 精密 (SMT が一括解決) | **over 方向を意図** (余分を含む・保証せず・過剰量と内包率を報告) |
| 出力 | 構造 + 条件 (SMT が一括解決) | 構造 (第 1 段階・前提) + **条件抽出 (第 2 段階・核: step A 分割設計 → step B 検証実行)** |

**Unsoundness の緩和**:
- 第 1 段階の AST 差分フィルタで「after に対応物がない差分ノード」を必須として保護 (単一セットアップでの誤 prune を構造的に回避)
- 共通ノードに潜む edge case (差分フィルタで救えないケース) は第 2 段階 (条件抽出) の T/NF/P/TF/V 同値分割テストで代表値網羅性を確保する二段構造
- pruning 候補の静的除外は **`@babel/types` の文法メタデータから自動導出する効率最適化** (ADR-0005) であり、正確性は Babel 型検査 / round-trip parse / EMIC による多層防御で担保する (実装詳細は [`code-map.md` §Pruning エンジン](code-map.md#pruning-エンジン))

---

### 第 2 段階: 条件抽出 — 同値分割テストによる適用条件の自動抽出 (本論文の核)

第 1 段階で導出したパターンに，**Selakovic の 5 前提条件 (T/NF/P/TF/V) を目標語彙として**適用条件を付与する (独自軸 C1〜C4 は廃止)．Selakovic Table 4 がパターンごとの手付けラベルを持つため，これが抽出の正解 (ground truth) になる．内部は **step A (分割設計) → step B (検証実行)** の 2 step で，**条件の確定は step B で起きる**．

> **入力側 / コード側の線引き**: 条件軸 (T/NF/P/TF/V) は**入力空間の分割**を担当し，入力値を変えて初めて露見する失敗 (例: `-0`, 上書き済み prototype) を捕まえる．一方，rename/巻き上げ/ASI/strict など**コードの形そのもの**で決まる失敗は入力分割では捕まらず，**第 1 段階 pruning ＋ 静的検査 ＋ 初回 before≡after 検証**が担当する．計算プロパティの動的名アクセス等は**判定不能**として定量報告する．

#### step A: 分割設計 — 固定カタログの一律適用

パターンのスロットに条件軸を当て，各軸を有限クラスに割り，各クラスを代表する入力を合成する．この時点では「どの軸が条件か」は未確定 (候補・仮説) であり，**確定は step B で起きる**．

**固定カタログ方式 ((A) のみ・循環回避の要)**: 代表値カタログは**スロットの静的な型・形で引き，パターンでは引かない**．「5 軸が何か」という一般語彙 (A) は使うが，「どのパターンにどの軸が効くか」という Selakovic Table 4 の答え (B) は step A に一切入れない．どのパターンに対しても該当型のラダーを**一律に当て**，効く軸の判定は step B の実行に委ねる (「条件の確定は step B で起きる」の実装上の担保)．これにより「軸を設計に使い同じラベルで評価する循環」のうち，最も痛い「答えを見て軸を選ぶ」経路を断つ (§妥当性の脅威)．

- **カタログはスロット型別に固定**: オブジェクト型スロットには T (型ラダー)・V (値ラダー)・P/NF/TF (汚染版・native 上書き版) を一律付与．数値スロットには数値の境界ラダー (`0`/`-0`/`-1`/`2.5`/`NaN`/`2^31` 等)．**P/NF/TF の汚染版・上書き版も汎用ラダーに固定で積む** (ガード無効化任せにしない．理由は step B)．
- **回し方は OFAT (一度に一スロット)**: 一つのスロットをラダー全体で振り，他スロットはクリーンな baseline に固定する．実行回数は `Σ_slot |ladder(slot)|` の線形．代償として**スロット間の複合条件は取りこぼしうる** (§妥当性の脅威に明記)．
- **FN の扱い**: 固定カタログが条件を出し損ねた場合，**パターン別に代表値を手当てして潰さない** (それは答え (B) の混入)．カタログの改善は**全パターンへ一律再適用するグローバル改善のみ**許し，理想的には実行前にプレレジする．潰さず限界として報告することが (B) 非混入の証拠になる．

条件軸ごとの確認手段:

| 条件 | 意味 | 確認手段 (TS / 素 JS) |
|---|---|---|
| **T** (Type check) | 式の型 | 明示的型注釈 + TS 推論 / `--checkJs` 推論・`typeof` ガード |
| **NF** (Native function not overridden) | 組込関数が上書きされていない | 上書き検査 (実行時に native か比較) — TS/JS 共通 |
| **P** (Prototype not overridden) | prototype のプロパティが上書きされていない | 上書き検査 / AST 走査 — TS/JS 共通 |
| **TF** (Third-party function not overridden) | ライブラリ関数が上書きされていない | 上書き検査 — TS/JS 共通 |
| **V** (Value check) | 式の値 | 型 + ガード / リテラル値・制御フロー内ガード |

**機械実装と評価の粒度**: NF/P/TF はいずれも機構的には「上書き検査」であり，**EMIC 内部では一軸として動かす**．ablation・再現率の報告時にのみ Selakovic の 3 ラベル (NF/P/TF) へ割り戻し，必要な粒度で寄与を併記する．

**原典を超える拡張**: T/NF/P/TF/V で覆えない条件 (V の provenance 拡張・数値特異点 `-0`/`NaN`/浮動小数/int32 等) は，独立カタログが必要性を実証したものだけを「原典を超える追加」として出自を明示して足す．無ければ future work．**先回りで軸を増やさない・出典の無い軸 (旧 C3 エイリアス等) は立てない．**

---

#### step B: 検証実行 — 代表値で走らせ条件を確定

step A の固定カタログの各代表値で EMIC を実行し，**どの条件クラスで等価性が壊れるか**を記録する．壊れた軸は，その入力を変えると等価性が崩れることを実証した条件として確定する．壊れなかった軸は「観測範囲で反証が得られなかった」として落とす (全入力の保証ではない．§難しさ)．**記号実行が全入力を網羅するのに対し，本手法は有限個の代表値で近似する**．

| 軸 | クラス分割の例 (Pattern #1) | 代表値 |
|---|---|---|
| T 型 | Object / Array / Map / null | `{a:1}`, `[1,2]`, `new Map()`, `null` |
| NF/P/TF 上書き検査 | 上書きなし / prototype 汚染 / `hasOwnProperty` 上書き | `{a:1}`, `Object.create({x:1})`, `{hasOwnProperty:()=>true}` |
| V 値 | 通常 / Symbol / 空 | `{a:1}`, `{[Symbol()]:1}`, `{}` |

**成果物 (deliverable) は core 構造 + 核条件の組** (射程 B・§目的)．一つの (before, after) から，**ガードを除いた核変換の構造**と，その核変換が要求する条件 (P/NF/V 等) を出す．両者は同じ変換 (ガード無しの核変換) を指すので整合する (これが C1〔構造と条件が別変換を指す不整合〕の解消)．ガード除去 (構造側) とガード無効化 (条件側) は同じ「ガード特定」機構を使う (機構は保留 → `open-questions.md`)．

条件の確定には**二層の比較を分析手段**として使う (出力の分類ではない):
1. **直接比較**: 書かれたままの before と after を代表値で比べる．防御的に書かれたインスタンスではガードが守るため乖離が出ず，条件が現れないことが多い．
2. **ガード無効化**: ガードを剥いだ核変換で比べる．ここで P/NF/V などのパターン条件が現れる．

> **ガード無効化は「帰属」であって「信号生成」ではない**: P/NF/TF の乖離を**生む**のは step A の固定カタログにある汚染入力の方であり，ガード無効化ではない．クリーンな入力ではガードを無効化しても結果は変わらない (継承プロパティが無いので列挙が変わらない)．ガード無効化の役割は，**防御的実装のせいで直接比較では隠れていた乖離を，ガードを剥がして露出させ，どの軸をそのガードが担っていたかを帰属する**ことにある (汚染入力 = 信号生成 / ガード無効化 = 軸への帰属，の分業)．

**帰属手順**: ガードを 1 本ずつ (複合ガードは項ごとに) `true` に置換し，露出した軸をそのガードの担当とする．妥当性チェックとして，**無効化版がクリーン baseline 入力で元と同じ挙動を保つ**ことを確認してから，汚染入力での乖離を信じる (別の場所を壊しただけの誤帰属を防ぐ)．どの部分式をガード候補とするか (核変換との区別) の基準は保留 (→ `open-questions.md`)．

**報告する所見 (結果) — discharger 帰属**: 直接比較で条件が出ず，ガード無効化で「P が必要」と出るとき，そのガードがその条件を discharge していたと帰属できる (例: `hasOwnProperty` ガードが P/NF を担う)．これは**手法の出力ではなく報告する所見**であり，「条件が不要なのではなく，コードが条件に対して防御している」という知見として扱う．**これら中間的な観察をどう表現・記録するかは実装時に決める** (中間出力の作り込みは前プロジェクトの複雑化要因なので story 段階では固めない → `open-questions.md`)．

**RQ1 が評価するのは成果物の核条件**: Selakovic Table 4 はパターン単位なので核変換条件と突き合わせる．書かれたまま (ガード付き) の直接比較結果と突き合わせると防御的インスタンスが軒並み空になり再現率が過小に出るため使わない．射程 B により RQ1 のラベル粒度は**パターン単位**となる (§RQ)．

#### Graded Soundness (段階的確信度)

検出時に T/NF/P/TF/V のどこまで確認できたかで確信度を付与:

| Level | 確認内容 | 想定する扱い (eslint 配布時) |
|---|---|---|
| 0 | パターン不一致 | 出力しない |
| 1 | 構文パターン一致 (第 1 段階) | suggestion |
| 2 | + T 型確認 | info |
| 3 | + NF/P/TF 上書きなし確認 | warning |
| 4 | + V 値確認 | error (高確信) |

**型情報による頭打ち**: 型情報が無い素の JS では，T (型) の確認手段が `typeof` ガードや AST 走査に限られ，**到達できる Level が頭打ちになる**．したがって本手法の有用性は **TS など型のある環境で強く出る**．素 JS は Level の上限が低いことを限界として明記する (§考察・§妥当性の脅威)．

**Level の運用ラベルは未評価の想定** (M7): 上表右列の suggestion/info/warning/error は eslint 配布時の**想定**であり，Level と実際の安全性 (適用したときに壊れない度合い) の相関は本論文では評価していない．とくに Level 4 =「error (高確信)」は運用上の主張であって実測ではない．検出ルール配布はスコープ外 (§Future Work) なので，この対応づけは仮説として提示する．

---

## 事前分析 + RQ

### 評価データの二本柱

Selakovic データセット (原論文は 98 件収集・本研究の利用可能母集団は 97 issue．差 1 件の除外理由は要確認 → `open-questions.md`) を役割の異なる二本柱で使う:

| 柱 | 対象 | 語ること | 指標 |
|---|---|---|---|
| **正確性** | 10 パターンに紐づく 16 issue | 生成手法 (条件抽出) の正確さ | Selakovic Table 4 手付けラベル (T/NF/P/TF/V) との precision/recall (= RQ1) |
| **拡張性** | 残り 81 issue (パターン外の個別最適化) | 既知パターンの外への適用可能性 + 未知パターンでの条件の妥当性 | 段階別通過率・条件出力の有無・`inconclusive` 率 (主指標・分母は未決定 → `open-questions.md`) + 大クラスタの条件 face validity (定性) |

81 issue は条件 GT を持たないため**正誤 (precision/recall) は測れない**．測るのは (i) パイプラインの適用可能性と (ii) 出力された条件の **face validity (解釈可能で妥当そうか)** であり，正確性の precision/recall とは同じ集計に混ぜない．

**位置づけの注意**: これは**外部妥当性ではない**．81 issue は Selakovic データ**内**でパターンの**外**なので，測れるのは「設計時に見ていない未知パターンへの汎化」(= **循環の緩和**に効く．§妥当性の脅威) であって，経年・出所バイアス (データ**外**の脅威) は緩和しない．経年/provenance の外部妥当性は当面行わず限界として明記する (点 3(b)・下記 §考察)．

拡張性の柱では，通過率の集計に加えて次の分析を行う:

1. **クラスタリング + 条件の face validity 確認 (パターン汎化)**: 81 issue をクラスタリングし (方法は未決定 → `open-questions.md`)，件数の多いクラスタを何個か手動で確認する．条件抽出まで届いた issue (ファネル上 pruning 通過は 34 件) について，**未知パターンでも出力条件が解釈可能で妥当か**を人手で判定する (GT が無いため correctness ではなく face validity)．固定カタログ方式 ((A) のみ) が未知パターンで妥当な条件を出せることは，「10 パターンを再生しているだけではない」の反循環証拠になる．人手判定はルーブリック事前登録 + できれば独立アノテータ 2 名で衛生を確保．**この評価の妥当性はクラスタリング設計に依存する** (表層特徴で切ると大クラスタが人工物になる)．範囲を見るため小クラスタ・単発も少数混ぜる．
2. **既知パターンのラッピング検証**: 81 issue のうち，既知 10 パターンを内部に含む (ラッピングしている) ものがどれだけあるかを検証する (ラッピングの判定基準は未決定 → `open-questions.md`)．

### 事前分析: パターン自動導出の妥当性確認

実行ベース pruning で Selakovic の 10 パターンを自動導出できるかを確認する．pruning 出力を，**両側で**評価する:

1. **取りこぼし側 (内包判定) = 保守性の測定**: 自動導出パターンが Selakovic Table 4 の手動パターンを内包しているか (AST tree-match)．**内包していれば over (安全側)，内包していなければ必要ノードを落とした under-approximation** であり，この内包率が「over 方向に倒れている」という主張の**経験的な検証**そのものになる (保証ではなく測定)．
2. **取りすぎ側 (過剰ノード量)**: 想定の形に対して余分に含むノード数・割合．over に倒れることの代償を定量化する指標．

**大ゼミ時点の結果 (取りこぼし側 = 保守性の測定)**: 等価判定を通過した 12 件中 9 件 (75%) で想定の形を内包 (抽出)．**残り 3 件は内包せず = under-approximation** であり，「常に over 側」という主張が現状では成り立っていないことを示す．内訳は P2 (繰り返し同型式の取り違え・**直せるバグ**) / P7 (**実装バグ**) / P10 (挿入型で pruning は before を削るため本質が after 側にある挿入は捕まえられない・**構造的限界**)．バグ 2 件を直せば内包は ~11/12 に届く見込みで，**残る限界は挿入型のみ**．よって主張は「保証」ではなく「**挿入型を除き経験的に over 側に倒れる (内包率で報告)**」と区切る．取りすぎ側 (過剰ノード量) は別途報告する．

データセット全体のファネル (スライド p20 補足): 97 issue → 前処理通過 79 → 等価判定通過 65 → 枝切り通過 45．うち既知 10 パターン該当は 16 issue (枝切りできたのは 11)，パターン外の個別最適化が 81 issue (枝切りできたのは 34)．

**評価指標の整理 (大ゼミ質疑)**: tree-match の内包判定は「over 側に倒れているか」の測定であり，これ単体では取りこぼし側しか見ない片側の指標である．**過剰ノード量**を併記して両側 (取りこぼし・取りすぎ) を測る．内包が 100% でない以上「保証」ではなく，内包率と過剰量を正直に出す (核を絞る精密化は行わない)．

### 事前分析: 条件抽出機構の実データ検証

第 2 段階 (条件抽出) の step A/B を実データで手動試行し，機構が機能するかを確認した (2026-06-19)．対象は for..in + `hasOwnProperty` ガードを `Object.keys` に置き換える 2 件 (Underscore 1222・Angular 7759_3)．実コードの before/after を最小コードに書き写し，P/V 軸の代表値を差し替えて EMIC を走らせた．

1. **直接比較では条件が出ない**: native 捕捉ガード付きの防御的実装を before/after そのまま比べると，全軸で等価になり条件が一つも現れなかった．Selakovic は Pattern #1 に P・NF を付けているが，実装が防御的なため直接比較では条件が見えない．
2. **ガード無効化で条件を帰属できる**: before のガードを無効化して比べると，P 軸代表値 (prototype に継承プロパティ) でのみ壊れ，そのガードが P 条件を担うと帰属できた．Angular 7759_3 の複合ガードでは，P 項を無効化すると P 軸のみ・V 項を無効化すると V 軸のみが壊れ，項ごとに軸を分解できた．

**含意**: 直接比較とガード無効化の**二層**が必要であり，これにより変換の核 (for-in→Object.keys) と条件を担うガード (hasOwnProperty = P) を**分離**できる．先行研究 (Selakovic) の手動ラベルと直接比較結果が一致しない事実が，二層分析の必要性を裏付ける．判定: Go．

### RQ

| RQ | 問い | 何で答えるか |
|---|---|---|
| **RQ1** | (before, after) ペアから T/NF/P/TF/V の適用条件を自動抽出したとき，Selakovic の手付けラベルをどれだけ再現できるか．較正ベースライン (全条件付与・静的ガード抽出) と LLM 直生成に対してどうか | Selakovic Table 4 の手付けラベルとの precision/recall．正解合わせは核変換条件 (成果物) に対して行い，**ラベル粒度はパターン単位** (射程 B・§step B)．各軸の寄与 (ablation〔条件オフ〕) と較正ベースライン・LLM 比較を同じ実験表から報告 |
| **RQ2** | 手法が捕捉できない失敗モードは何か，LLM と比べて難所はどこにあるか | RQ1 の失敗を事後分類する error analysis ＋ 本手法 vs LLM の難所マップ．失敗モードカタログ (36 件) は「負例刺激」として与え感度を測る (限界を測る物差しには使わない) |

- **事前分析** = 第 1 段階が動くかの確認 (前提条件．step A/B 検証は RQ1 へ吸収)
- **RQ1** = 第 2 段階 (条件抽出) の正確さ：Selakovic の外部公開ラベルを正解として再現率を測る．**ablation (条件オフ)・較正ベースライン (全条件付与・静的ガード抽出)・LLM 比較を内包**．**対象は 10 パターン紐づき 16 issue (正確性の柱)**
  - **較正ベースラインの役割**: ライバル手法との benchmarking ではなく (先行する JS 手法が存在しないため)，「問題が非自明で本手法の実行ベース機構が必要」であることを妥当なベースラインに対して較正する positioning．**全条件付与** = 常に 5 軸全部を出す素朴上界 (precision の床．本手法が軸を選べているかを見る)．**静的ガード抽出** = before のガードを実行せず構文的に読んで条件へ写す素朴法 (「なぜ実行するのか」の直接検査．非ガード条件・値条件を取れないので recall 上限 = ガード済み条件の割合)．条件ゼロ ablation は SYDIT の代役ではなく自手法のアブレーションであり，SYDIT は related work の歴史的動機に留める (**「SYDIT 相当」の看板は撤回**)
- **RQ2** = 手法の限界：error analysis で失敗を類型化し，LLM との難所マップで本質 (両方落ちる) / 改善余地 (本手法だけ落ちる) を切り分ける
- **拡張性** = パターン外の残り 81 issue で，手法が既知パターンの外へどこまで適用できるかを通過率・出力有無で示す (条件 GT なし・正誤は測らない．RQ への編成は未決定 → `open-questions.md`)
- **循環の緩和 (パターン汎化・限定的)** = 拡張性の柱で，81 issue (パターン外) の大クラスタの条件 face validity を確認 (§評価データの二本柱)．**示せるのは「10 パターン固有の〈パターン→条件〉対応を暗記していない」という限定的証拠 (transfer feasibility) まで**．語彙 (T/NF/P/TF/V) 自体のリークは同じ語彙を出す以上これでは検証できない (残留・§妥当性の脅威)．「循環を断つ」とは主張しない
- **外部妥当性 (経年/provenance)** = **当面行わない (点 3(b))**．案 A (Selakovic データ外 OSS での自動再発見) は保留し，経年・出所バイアスは限界として明記．論文の質が要求すれば案 A の復活を検討 (→ `open-questions.md`)

### RQ1 の検証方法

各パターンの同値分割テスト結果を集計:

| パターン | 正常 | T 違反 | 上書き違反 (NF/P/TF) | V 違反 |
|---|---|---|---|---|
| #1 for..in → Object.keys | 等価 ✓ | 非等価 ✓ | 非等価 ✓ | 等価 (不要) |
| #3 String() → ''+x | 等価 ✓ | 非等価 ✓ | 等価 (不要) | 非等価 ✓ |
| ... | ... | ... | ... | ... |
| **寄与率** | — | 10/10 | 6/10 | 8/10 |

上書き違反は必要に応じて NF/P/TF に分解して寄与を併記する．

**RQ1 の測定の射程と単位 (査読対応)**:
- **採点対象は「5 カテゴリ (T/NF/P/TF/V) のうちどれが必要か」の同定** (C2)．「T が必要」まで当てても許容型集合・値領域・論理結合まで正解とは限らないため，RQ1 の precision/recall は**前提条件の種別同定**を測るものであり，任意述語としての「適用条件そのもの」の正誤ではない．step B が出す具体的な値クラス (どの代表値で壊れたか) は**定性的な補足**として併記し，full-predicate の正確さは主張しない．
- **主スコアはパターン単位 (N=10)** (M1)．16 issue は 10 パターンにクラスタ化されており，同一パターンに同じラベル集合を複製して issue 単位で集計すると件数の多いパターンが不当に重くなる．よって主 precision/recall はパターン単位で計算し，**同一パターン内の複数 issue から同じ核変換条件を安定して再構成できるか (再現安定性) は別指標**として報告する．
- **2 つの estimand を分ける** (M2)．既知 16 issue のうち第 1 段階 (構造) を通過したのは 11 件．(i) **component 評価**: 全 16 に正解構造を供給し条件抽出だけの正確さを測る．(ii) **end-to-end 評価**: 自動構造を使い，通過しなかった件を FN として数える．両者を混同せず別々に報告する (survivorship bias 回避)．

**脅威: T/NF/P/TF/V を設計に使い Selakovic ラベルで評価する循環**: RQ1 は「設計で採用した軸 (T/NF/P/TF/V) を正解ラベルとしても使う」という形になるため，「当然再現できる設計になっている」という批判がありうる．緩和は二段:
1. **ラベルの独立性**: Selakovic Table 4 の手付けラベルは著者が本研究とは独立に手動付与したものであり，自作ラベルではない．
2. **パターン汎化 (拡張性の柱・限定的)**: パターン外 81 issue で，設計時に見ていない未知パターンでも妥当な条件が出るかを確認する (§評価データの二本柱)．**これで示せるのは「10 パターン固有の対応を暗記していない」という限定的証拠まで**であり，語彙自体のリークは断てない (残留リークは下の脅威表で「断てない」と明記)．外部 OSS 適用 (案 A) は当面見送り・§考察．なお RQ2 (失敗モードカタログ) の役割はカタログの妥当性ごと再検討中で凍結 (→ `open-questions.md`)．

### 考察で扱うもの

- **循環の緩和はパターン汎化で限定的に担う**: データ**内**・パターン**外**の 81 issue で，未知パターンでも妥当な条件が出るかを確認する (§評価データの二本柱)．これは循環のうち「10 パターン固有の対応を暗記しているだけでは」という層への**限定的な反証**であり，語彙 (T/NF/P/TF/V) 自体がデータ由来という層は断てない (脅威表に明記)．「主たる反証」とは呼ばない．
- **外部妥当性 (経年/provenance) は当面見送り (点 3(b)・限界として明記)**: 案 A (Selakovic データ外 OSS からの (before,after) 収集 → tree-match で既知パターンの自動再発見) は保留する．これにより外部での**構造再発見と条件確認の双方**を当面行わないことになり，2016 年データ・単一収集方法に由来する経年/出所バイアスは**限界として正直に記す** (Future Work)．論文の質が要求すれば，EMIC を判定に使わない tree-match (構造) + 人手オラクル (条件) の形で案 A を復活させる (→ `open-questions.md`)．
- **条件のズレの切り分け ((a)/(b))**: **主結果は原ラベルに対する生の precision/recall で固定し変更しない** (M4)．食い違いの再解釈は confirmation bias を避けるため**事前登録した手続きに沿った補助的定性分析に限定**する: (a) 真の穴 (FN・手法の限界 → RQ2) ／ (b) 正当な差 (手法が人手より細かい)．**射程 B で RQ1 はパターン単位・核変換条件に固定したため，「防御的インスタンスなので条件が出ない」という instance 級の弁明は RQ1 には持ち込まない** (旧射程 A の残滓・除去済み)．minimality (overly weak/strong) の語彙を使う場合のみ Mongiovi 2018 を任意で引く．
- **Selakovic 分類の穴 (独立カタログ)**: 失敗モードカタログで Selakovic の T/NF/P/TF/V 分類自体の取りこぼしを独立に示す (本手法の限界 RQ2 とは別の貢献として分離)．
- **型情報の影響**: TS / JS+Doc / JS での Level 到達率の変化を観察 (素 JS は Level 頭打ち)．

---

## 貢献

### 新規に作るもの

| # | パイプライン | 内容 | 新規性 |
|---|---|---|---|
| 1 | [2] | **T/NF/P/TF/V 条件抽出 + 同値分割テスト (本論文の核)** (内部で EMIC を使用) | Selakovic が「今後の課題」とした手動前提条件 (T/NF/P/TF/V) をそのまま自動化．記号実行の代替として入力空間を有限分割．**JS で 10 年間誰も達成していない** |
| 2 | [1] | **実行ベース Hydra 式 pruning** (内部で EMIC を使用・出力は over 方向を意図/保証せず) | Hydra の SMT を JS 向け実行ベースに転換．**構造を前提として供給する工学的貢献であり，条件抽出 (核) と対等な学術的新規性としては強調しない** (M7) |
| — | 共通基盤 | **EMIC (入力限定等価確認器・4 オラクル)** | [1] と [2] の内部で使用．独立したステップではなく基盤ツール |

(検出ルール ts-eslint の自動生成は本論文スコープ外 → §Future Work)

### 本論文の学術的貢献

1. **JS での N=1 条件付き検出パターンの「必要な前提条件の種別」の自動同定**: Selakovic が手動で付与し「自動化は今後の課題」とした前提条件の**種別 (T/NF/P/TF/V のどれが必要か)** を，構造 (前提) の上で自動同定する．Selakovic Table 4 を正解として再現率を測れる．**採点は種別同定の粒度**であり，許容型集合・値領域といった full-predicate の抽出まで正解と主張はしない (具体の値クラスは定性補足・§RQ1)．Selakovic が「今後の課題」とした自動化への**初の JS 解法**である点が新規性 (問題の初提起ではない・§貢献冒頭の位置づけ)．
2. **各条件軸の寄与 (load-bearing 度) の定量化 (ablation)**: 各条件軸 (T/NF/P/TF/V・必要に応じ NF/P/TF へ分解) が，どのパターンで等価性維持に効いているかを同値分割テストで計測 (RQ1 の軸別分解)．**「FP 削減」は主張しない**: FP は検出概念であり，検出器 (eslint) はスコープ外・RQ1 の 16 issue は全て正例で負例が無いため，展開時の実 FP は測れない．負例に対する違反感度を FP の機構レベル代理として出す件は RQ2 (負例カタログ) の再設計に紐づけて保留 (→ `open-questions.md`)．
3. **記号実行の代替としての同値分割テストの提案**: SMT が使えない JS で，条件軸による有限分割で等価性検証を近似する手法．
4. **限界の正直な定量化 (正確さ)**: 構造の近似方向を保証しないことを明示し内包率 (over 側か) と過剰ノード量 (over の代償) を報告，検出漏れを保証しないことを明示，素 JS の Level 頭打ちを定量化する．「できないことの境界」を曖昧にしない．

**勝ち軸**: 新規性 (初の JS 解法) ＋ 正確さ (限界の正直な定量化)．汎化は拡張性の柱 (パターン外 81 issue での条件 face validity) が補助的に支える (外部 OSS 適用は当面見送り・§考察)．

**本研究の位置づけ (既知未解決問題への初の JS 解法)**: 本研究は「新問題を初めて提起する」型ではない．**解く価値のある問題は既に確立している**——Selakovic (ICSE 2016) が前提条件の自動チェックを「今後の課題」と明示的に提起し，Hydra (OOPSLA 2024) が LLVM IR で構造+条件の自動導出を解いた．本研究の位置づけは，この**既知の未解決問題に対する初の JavaScript 解法**である．核の貢献は「移植」ではなく「機構の作り替え」にある: **Hydra の自明な解法 (SMT) は JS の動的意味論ではパスが無限爆発して使えない**ため，検証機構を SMT から**実行ベースの有限分割**に置き換えた (§背景・§難しさ)．**先行する JS 手法が存在しないため比較は positioning** (妥当なベースラインに対する実行機構の追加価値の較正) であって，SOTA を数値で上回る benchmarking ではない (詳細 `drafts/evaluation-design-b-type.md`．同 draft の「B 型/新問題提起型」framing は本節に更新)．

---

## 難しさ

### 本研究固有の困難

| 困難 | 内容 |
|---|---|
| **SMT が使えない** | JS の動的意味論は SMT の決定可能断片に落ちない．実行ベースに転換する必要がある |
| **Unsound pruning** | 実行ベースではテスト入力のみ保証．AST 差分フィルタで差分ノードを必須保護し、共通ノードの edge case は第 2 段階 (条件抽出) の T/NF/P/TF/V 同値分割で緩和 |
| **構造出力の近似方向は保証されない** | pruning は over 側 (余分を含む) に倒れる想定だが，実行ベースゆえセットアップが弱いと必要ノードを落とす under 側の誤りもありうる (健全性は保証しない)．over かは内包率で経験的に測り，挿入型が構造的限界．構造は前提なので精密化せず量と限界を正直に報告する立場を取る |
| **セットアップの代表性** | テスト入力が入力空間を十分に網羅しているか保証できない．固定カタログが条件を出し損ねる (FN) 場合はパターン別に潰さずグローバル改善のみとし限界を報告 |
| **OFAT の複合条件取りこぼし** | step A は一度に一スロットのみ振る (OFAT) ため実行回数は線形だが，スロット間の複合条件を取りこぼしうる．限界として明記し，必要なら二次組合せのみ追加 |
| **素 JS の Level 頭打ち** | 型情報が無いと T (型) の確認手段が限られ到達 Level が下がる (NF/P/TF の上書き検査・V は実行時に確認でき型情報に依存しにくい)．有用性は型のある環境で強い |
| **検出漏れの保証は不可能** (大ゼミ質疑) | 検出漏れ (false negative) ゼロを保証するツールは動的言語ではほぼ作れない (サーベイ継続中)．本研究の主張は「保証」ではなく「**手法として漏れがほとんど無いことを示す**」形に置く: 実行観測 + T/NF/P/TF/V 同値分割で系統的に入力空間を覆い，漏れが残る箇所 (判定不能・パターン外) を定量報告する |

**この研究の一言サマリ (大ゼミ質疑への答えとして整備)**: 動的型付け言語では SMT が行う記号実行が使えない (パスが無限に爆発する) のが問題．本研究は，**実際に動かした結果を観測しながら適用条件を取り出す**ことでこれを解決する — 検証手段を SMT から実行ベースの EMIC に置き換え，全入力の保証は T/NF/P/TF/V による有限分割の代表値検証で近似する．

### 評価哲学: N=1 型としての外部妥当性

本手法は **N=1 型** (1 ペアから条件付きパターンを作る) であり，各ケースは独立した定性実証である．**ただし「入力が 1 ペア」であることと「評価の標本設計を考えなくてよい」ことは別**である (M6): 手法が学習器でない (母集団性能ではなくケース実証を重視する) としても，**どの変換・条件・構造・失敗特性で機能するかを言うには評価事例の選択と依存性 (16 件が 10 パターンにクラスタ化・81 件も大クラスタ中心) を扱う必要がある**．よって「壊れない」「汎化する」「漏れがほとんど無い」という率的・一般的主張は避け，**主張の射程を事例群に限定する**．汎化の確認も「複数 OSS から率を出す」N≥2 学習型ではなく「N=1 試行の少数独立反復」で行うが，それは統計評価の免除ではなく主張範囲の限定である．経年/provenance を突く外部 OSS 適用は当面見送り，限界として明記する (点 3(b))．

### 妥当性の脅威

| 脅威 | 緩和策 |
|---|---|
| EMIC をパイプラインと評価の両方で共用 | ネガティブセットアップの一部を手動確認 |
| **セットアップコードは未評価の人手入力** (M5) | 実際のパイプライン入力は「(before, after) ペア **+ セットアップコード**」であり，初回等価判定・pruning・条件観測はすべて setup に依存する．「before/after のみから」という表現は不正確．setup の作成者・作成規則・**Selakovic ラベルを参照せず作るか (label-blind)**・issue 固有知識をどこまで含めるか・作成コストを規定する必要がある (プロトコル未定 → `open-questions.md`)．規定しないと「どこまでが自動抽出の成果か」が判別できず RQ1 の循環にも直結する |
| **Selakovic データの経年 (2016 年)・出所バイアス** | **緩和策は当面持たない (点 3(b))**．外部 OSS 適用 (案 A) を見送り，post-2016 / 非 Selakovic コードでの検証は Future Work として限界に明記する．論文の質が要求すれば案 A を復活．注: パターン外 81 issue の汎化確認は Selakovic データ内なので経年は緩和しない (循環のみ緩和) |
| **構造の近似方向は保証されない (over/under 両方向に誤りうる)** (大ゼミ質疑) | 「保守的 (取りこぼしゼロ)」は保証ではなく設計意図．実行ベースゆえセットアップが弱いと under-approximation (必要ノードの取りこぼし) が起きる (現状 内包 9/12・バグ修正後 ~11/12・残る限界は挿入型)．内包率で over 側かを経験的に測り，過剰ノード量で over の代償を測る．構造は前提なので精密化せず限界を明記 |
| **AST tree-match 評価は内包判定のみ** (大ゼミ質疑) | 過剰ノード量の指標を追加して両側 (取りこぼし・取りすぎ) を測る |
| **RQ1 のデータセット内循環** | 3 段で緩和: (1) **固定カタログ方式** — step A はスロット型でカタログを引き，「どのパターンにどの軸が効くか」(答え) は使わない ((A) のみ・§step A)．「答えを見て軸を選ぶ」経路を断つ．(2) FN をパターン別に潰さずグローバル改善のみ許すことで (B) 非混入を担保．(3) Selakovic 内で組み立て・評価する残留循環は，パターン外 81 issue での汎化確認 (未知パターンで妥当な条件が出るか・§評価データの二本柱) で緩和する (外部 OSS 適用は当面見送り)．残留リーク (語彙 T/NF/P/TF/V の固定・EMIC のパイプ/評価共用) は断てないと正直に明記 |
| **素 JS の Level 頭打ち** | 型情報が無いと到達 Level が下がる．TS / JS+Doc / JS で Level 到達率を比較し，有用性が型のある環境で強いことを明示 |
| EMIC が一部の candidate を「中身まで実行できず判定不能 (`inconclusive`)」にする (dep 不在 / Ember の AMD loader / workload が薄い等) | `equal` = positive な等価エビデンスで確認・`not_equal` = 差を観測・`inconclusive` = 検証不能、と verdict を分け、「EMIC が著者判断と一致した」の分母は `equal`+`not_equal` の確認済み分のみにし `inconclusive` は別途「検証カバレッジ」として報告する。`not_equal` のうち記録 Proxy 干渉等の checker artifact 由来のものは手動レビューで角に置く (= ADR-0018 / ADR-0013 §threats)。当面 ~67 件確認済 / ~6 件 not_equal (うち 4 件が genuine な検出、うち 1 件 = angular-10351 は著者判断との既知の不一致) / ~33 件 inconclusive |

---

## 用語の再検討: 「等価性検証器」→ EMIC への改名 (大ゼミ質疑)

**指摘**: 「等価性検証器」という名前は，形式手法系の研究者から「等価性とは何か (どの同値関係か・何を保証するのか)」を突かれる確率がとても高い．実態は「O1〜O4 の観測チャネル上で，有限個の入力に対して差が出ないことを確認する」道具であり，形式的な等価性 (全入力・全文脈での同値) は保証していない．

**一旦決定 (指摘されたら差し戻す)**: 改名する．新名称は **EMIC (入力限定等価確認器, Equivalence Modulo Inputs Checker)**．根拠・詳細は `drafts/renaming-equivalence-checker.md`．形式手法系から本質的な反論が出た場合は再検討する．

- **根拠**: Le, Afshari, Su. *"Compiler Validation via Equivalence Modulo Inputs."* PLDI 2014．EMI (Equivalence Modulo Inputs) は入力集合 $I$ に限定した等価 ($\forall i \in I.\ \llbracket P \rrbracket(i) = \llbracket Q \rrbracket(i)$) を定義し，有限テスト入力による動的確認の理論的位置づけとして確立している．
- **命名の意図**: 「入力限定」= 有限の $I$ に限定した等価であることを名称に含める / 「等価」= 類似性ではなく等価を主張する強さを維持 / 「確認器 (Checker)」= 形式証明器 (Verifier) ではなくテストによる確認であることを示す．
- 突かれた場合の回答: 「EMI (Le et al., PLDI 2014) の枠組みに基づく動的確認．有限の代表入力クラスに限定した等価であり，全入力の形式保証ではない．」

<details>
<summary><strong>検討した候補 (決定までの記録)</strong></summary>

| 候補 | 強さ | 備考 |
|---|---|---|
| **EMIC / 入力限定等価確認器** | 中強 | **採用**．EMI (PLDI 2014) を根拠に「入力限定の等価」を正確に名指しできる |
| 振る舞い保存 (behavior preservation) 検証器 | 中 | リファクタリング分野の定訳．スライド v2 の「振る舞い」背骨と整合 |
| 観測的一致 / 振る舞い一致 検証器 | 中 | 「観測した範囲で一致」という実態に正直 |
| 観測等価 (observational equivalence) 検証器 | 強 | 形式手法の既定義語なので結局同じ指摘を受けるリスク |
| 差分検出器 / 振る舞い差分検証器 | 中弱 | 「差が無いことの確認」に近いが，等価側の主張が消える |

</details>

---

## スケジュール

締切は置かない．投稿先・時期は質を優先して再検討中．

大ゼミで対外的に示した展望 (スライド p19「展望 (修論まで)」): ①条件抽出の実装 ②検出ルール (eslint) として配布し OSS に実適用．①は本論文の核として実装する．②の配布は本論文スコープ外 (修論までのコミットメント・§Future Work)．

### 当面の作業順

1. **副作用の観測 (O2/O4) 方式の spike** — 実行前後の状態差分が有力
2. **過剰ノード量メトリクスの実装 + 内包率の集計** — over 方向の主張を測る (保証でなく測定) のに必須
3. **第 2 段階 条件抽出の実装 (step A 分割設計 → step B 検証実行)** — 本論文の核 (NF/P/TF は上書き検査一軸で実装)
4. **RQ1 ablation** — 各条件軸の寄与 (load-bearing 度) を定量化 (対象は正確性の柱・16 issue)
5. **パターン外 81 issue の拡張性バッチ実行 + 大クラスタの条件 face validity 確認** — 段階別通過率・条件出力の有無を集計し，大クラスタの条件が妥当かを人手確認 (クラスタリング方法・主指標・分母の決定後)
6. (見送り) 外部 OSS 適用 (案 A) — 点 3(b) により当面行わない．論文の質が要求すれば復活

---

## Future Work

### 構造パターンの精密化 (旧 [1.5] 核抽出)

pruning は「消しても等価性が保たれるか」でしか削れないため，**構造パターンには改善の核以外の文脈が残る** (over 側に倒れたときの余分)．

例: 本当に欲しいのは `keys[i] % 2 === 0` の部分だけでも，囲っているループを消すと等価性が崩れるので削れない:

```javascript
function () {
  for (var i = 0; i < keys.length; i++) {
    keys[i] % 2 === 0;   // ← 欲しいのはここだけ
  }
}
```

本論文ではこの過剰を「引き受けて定量報告する」立場を取るが，将来的には**構造パターンから核を絞り込む精密化**で過剰量を減らせる可能性がある (候補: after 側との差分ノードを核の種にする / 文脈ノードを段階的にワイルドカード化して検出器側で吸収する 等)．`open-questions.md` に登録済み．

### 検出ルール (ts-eslint) の自動生成と OSS 配布

パターン導出 → ESLint 配布の一貫パイプラインは前例がない．本論文では条件抽出の評価に集中するためスコープ外とし，検出ルールの自動生成と実 OSS への配布は将来課題 (修論までのコミットメント) とする．

### パターン限定の記号実行 (sound 検証への拡張)

本論文の第 2 段階 step B は同値分割テスト (代表値による具体的検証) だが，構造パターン + T/NF/P/TF/V 条件でパターン内の実行パスが有限に限定されるため，**ExpoSE 等の JS 記号実行ツールで sound に検証できる可能性**がある:

- 構造パターンが制御フローを固定 → ループ回数・分岐が限定
- T/NF/P/TF/V が入力の型・上書き状態・値を限定 → 各分岐のパスが削減
- **「JS 全体は記号実行不能だが，パターン + 条件で限定された断片なら可能」**

これが実現すれば，本論文の unsound な検証を sound に置き換えられる．

### カテゴリ拡張

性能パターン (Selakovic) は第一実証．同じフレームワークで可読性・バグ修正・モダン化のパターンにも適用可能．

---

## 参考文献

**パターン導出の系譜**:
- **SYDIT** FSE 2011 — N=1．条件なし → FP だらけ
- **LASE** ICSE 2013 — SYDIT 批判．N≥2 に移行
- **Hydra** OOPSLA 2024 — N=1 + SMT で構造+条件を自動導出．LLVM IR 限定
- **Getafix** FSE 2019 / **REVISAR** ICSE 2018 / **PyCraft** FSE 2024 — 複数例前提

**前提条件**:
- **Selakovic & Pradel** ICSE 2016 — 構造+条件を手動特定．「自動チェックは今後の課題」
- **ConflictJS** ICSE 2018 — NF/P/TF の部品技術
- **Gao et al.** ICSE 2017 — 型チェックで JS バグの 15% を検出可能

**評価手本 (SE 系・既知問題/直接の対抗手法なしを oracle 再現+妥当なベースラインで正当化した前例)**:
- **SafeRefactor** (Soares et al., SBES 2009) — N=1・既知 oracle 再現・precision/recall・ベースライン不在の正当化 = RQ1 の型
- **Soares et al.** TSE 2013 (Automated Behavioral Testing) — 失敗モードの言語非依存な体系化 = RQ2 の参考
- ベースライン不在を埋めた前例: REVISAR ICSE 2018 / Getafix OOPSLA 2019 / Refazer ICSE 2017 / ConflictJS ICSE 2018
- (任意) **Mongiovi et al.** TSE 2018 (Overly Strong Preconditions) — minimality / overly weak-strong の語彙を考察で使う場合のみ引く

**ESLint**: Tómasdóttir et al. TSE 2020

**記号実行**: ExpoSE (Loring et al., SPIN 2017) — JS 向け動的記号実行

**理論基盤**: Alive-Infer PLDI 2017 / Cerna & Kutsia IJCAI 2023 (anti-unification survey) / **Le, Afshari, Su, "Compiler Validation via Equivalence Modulo Inputs", PLDI 2014** (EMI = EMIC 命名の根拠)

**条件抽出の方法論基盤** (AST 断片を ablate × 軸代表値で EMIC 検証し、核/条件(ガード)/デッドコードを分離。建て付け = EMI を理論的傘に、変換機構・最小化目的・代表値設計の 3 柱):
- **傘 (EMI)**: Le-Afshari-Su PLDI 2014 (上記) を「before≡after を代表値入力集合 I 上で検証する」枠組みの根拠に再利用。続編 Athena (OOPSLA 2015) / Hermes (OOPSLA 2016) は mutate 戦略の発展系
- **変換機構 (mutation 演算子の流用・testing ではない)**: DeMillo-Lipton-Sayward 1978 (起源) / Jia-Harman TSE 2011・Papadakis et al. 2019 (survey) / **Deng-Offutt-Li ICST 2013 (SDL = 文削除演算子)** / PIT (Coles et al. ISSTA 2016, Remove Conditionals の実装前例)
- **最小化の目的 (delta debugging / slicing)**: Zeller ESEC/FSE 1999・Zeller-Hildebrandt TSE 2002 (ddmin) / **Misherghi-Su ICSE 2006 (HDD = AST 構造を尊重した縮約)** / Weiser TSE 1984 (slicing)
- **代表値設計 (同値分割)**: Myers 1979 (用語原典) / Ostrand-Balcer CACM 1988 (category-partition) / Claessen-Hughes ICFP 2000 (QuickCheck = 各クラスの witness 生成)
- 注: 観測等価 (live set 射影) は素朴 EMI (全状態一致) の緩和であり、その精密化は io-based-equivalence / SLACC 系と組で引く (別途)
