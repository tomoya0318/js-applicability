# AGENTS.md

エージェントが毎回「初見」で入る前提で、**コードと設定ファイルを読んでも分からないことだけ**を書く。

## WHY — なぜこの規律があるか

前身となるプロジェクトは 2 つの失敗で終わった。**過度な実装によるスパゲッティ化**と、**テストの形骸化**。

根は同じ ——「**未決定をコードで先送りする**」こと。決めていないから両方できるように抽象化して分岐が増え、
決めていないから期待値が書けず、出力を固定するだけのテストになる。この研究は未決定が 20 項目近くある。

したがって:

1. **未決定はハードコードする。** 設定項目・フラグ・strategy にしない。「将来こうなるかも」で抽象化しない
2. **fallback を書かない。** 対応外の入力は `unsupported` / `inconclusive` を返す。**これは仕様であってバグではない**
3. **迷ったら作らない。**
4. **テストの期待値を書き換えない。** `corpus/` への追加のみ許す（ラチェット）。
   赤を緑にするために期待値を直した瞬間、テストは形骸化する

**緩和案は一つずつは合理的に見える。** 上を崩す提案には理由を求めること。

## 未決定に出会ったら

ハードコードするだけでは、何を先送りしたかが見えなくなる。**必ず TODO として残す。**

- **実装の局所的な選択** → その場にハードコードし、`// TODO(未決定): 理由と、決まったら何をするか` を書く。
  一覧は `grep -rn "TODO(未決定)" src scripts` で得る。索引ファイルを作らない
- **研究の設計に関わる選択** → `research-handoff/blocking-decisions.md` に追記する
- どちらの場合も**分岐・フラグ・設定項目にしない**。TODO は「決めていない」の記録であって、
  「両方できるようにしておく」の言い訳ではない

## WHAT — 地図

| | |
|---|---|
| `README.md` | パイプラインと実行手順 |
| `research-handoff/blocking-decisions.md` | 未決定の置き場。**コードに分岐を作る代わりにここへ書く** |

```
src/        純粋なロジック。副作用なし・固定パスを知らない
  types.ts    verdict / reach / status の唯一の定義
  io.ts       JSON 読み書き。ステージ共通の不変条件をここで強制する
  emic/       隔離実行と 4 オラクル観測 (prune と cond が内部で使う共通基盤)
  prep/ prune/ cond/
scripts/    ステージの実行ファイル → scripts/AGENTS.md
corpus/     答え付きの合成テストケース
test/       e2e テストのみ → test/AGENTS.md
```

`utils/` `common/` `helpers/` `core/` `config/` と barrel (`index.ts`) を作らない。
行き場のないコードの避難所になり、依存が全方向に伸びる。共通処理は `src/io.ts` だけ。

`src/` 直下にディレクトリを足すのは、ステージが増えたときだけ。

## HOW — 検証

```sh
pnpm check   # tsc --noEmit + oxlint --type-aware + oxfmt
pnpm test    # corpus を流す e2e
```

テストランナー・リンタ・フォーマッタを追加しない。型・未 await・import 循環・依存方向・整形は
すべて `pnpm check` が決定的に見る。**だからこの文書はそれらに触れない。**

## コードを読んでも分からないこと

- **Node のバージョンは EMIC の独立変数。** 実行環境が変わると等価判定の結果が変わる
  (Node 22 と 24 で結果が変わった前例がある)。**結果 JSON に必ず記録する。** 版は `mise.toml` だけで指定する
- **`Math.random` と `Date` を lint で禁止しているのは、実行結果が論文の数字になるため。**
  run メタデータの記録だけ disable コメントで明示的に許可する
- コメントは「なぜそうしたか」だけ書く。`jsdoc` は oxfmt / oxlint の両方で意図的に無効にしてある。
  **docstring 文化を作らないための決定**なので `jsdoc` プラグインを追加しない
- `@babel/traverse` は ESM から使うと `.default` が要る場合がある

## 警報ライン

超えたら機能追加ではなく削る対象を探す。

`src/` 3,000 行 / `types.ts` 300 行 (超えたら中間表現の作り込み) / npm 依存 8 個 / `scripts/` 4 ファイル

## この文書

300 行未満、1 ファイルに保つ。`CLAUDE.md` は symlink。
特定ディレクトリでしか意味を持たないルールは、そのディレクトリの `AGENTS.md` に置く (nearest-wins)。
`.claude/rules/` は作らない — Codex が読まず、結局こちらにパス一覧を書くことになる。

このリポジトリ限定の skill は `.agents/skills/<name>/` が正本で、`.claude/skills/<name>` は
そこへの symlink を skill ごとに貼る。`CLAUDE.md` と同じ理由で、正本を 1 つに保つ。
